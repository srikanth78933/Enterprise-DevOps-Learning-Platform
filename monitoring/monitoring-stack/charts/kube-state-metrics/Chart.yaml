apiVersion: v2
name: kube-state-metrics
description: Exposes Kubernetes object state (pod status, restarts, deployment replicas, etc.) as Prometheus metrics
type: application
version: 1.0.0
appVersion: "2.13.0"
